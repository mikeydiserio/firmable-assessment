import styled from '@emotion/styled'

export const Footer = styled.footer`
  width: 100%;
	height: 80px;
	background-color: transparent;
	position: relative;
	bottom: 0;
	left: 0;
	overflow: hidden;
	text-align: center;
`
